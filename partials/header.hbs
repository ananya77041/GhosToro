<header id="header">
	{{#if @blog.logo}}
		<a class="image-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}} Logo" /></a>
	{{else}}
	    <a class="logo" href="/">
	        inunDATAd<span>data science</span>
	    </a>
    {{/if}}
    {{^is 'home'}}
    	{{> menu}}
    {{/is}}
</header>
